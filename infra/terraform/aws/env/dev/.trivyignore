# 開発環境で許容するルール
# 本番環境ではこれらのチェックを有効にし、HTTPS化・Flow Logs有効化・Egress制限を行うこと

# ALBが公開されている（公開APIのため意図的）
AVD-AWS-0053

# HTTPSを使っていない（dev環境はドメイン/証明書なし）
AVD-AWS-0054

# Egress 0.0.0.0/0（ECR/CloudWatch等への通信に必要）
AVD-AWS-0104

# VPC Flow Logs無効（dev環境ではコスト削減のため省略）
AVD-AWS-0178
